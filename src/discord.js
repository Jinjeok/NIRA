import discord from 'discord.js';

export const {
    ActionRowBuilder,
    ActivityType,
    AttachmentBuilder,
    ButtonBuilder,
    ButtonStyle,
    Client,
    Collection,
    EmbedBuilder,
    GatewayIntentBits,
    MessageFlags,
    PermissionsBitField,
    REST,
    Routes,
    SlashCommandBuilder,
    WebhookClient,
} = discord;

export default discord;
